{
  "name": "touchgrass",
  "private": true,
  "scripts": {
    "dev": "live-server .. --host=0.0.0.0 --port=8443 --no-browser --https=live-server-https.js --entry-file=render/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
